# Example Export

- `example_pack_template.zip` is a structure-only sample produced for this repo deliverable.
- It contains a placeholder empty `.ogg` and is not intended for real in-game playback.
- Use the web app export flow to generate valid packs with real recordings.
